import streamlit as st

st.set_page_config(page_title="IFS Parts Mapper (v1)", layout="wide")

st.title("IFS Parts Mapper (v1)")
st.caption("Session-only. No accounts. No autosave. Export/Import JSON is the only persistence.")
st.info("Setup OK. Next step: add the v1 data model + validation (no Self node, no inference).")
